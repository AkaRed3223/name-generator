/*
import 'package:namer_app2/main.dart';
import 'package:test/test.dart';

void main() {
  group('Favorites', () {
    test('should initialize with 0 favorites', () {
      expect(MyAppState().favorites.length, 0);
    });

    test('should have 1 favorite when 1 toggle is activated', () {
      final myAppState = MyAppState();
      myAppState.toggleFavorite();
      expect(myAppState.favorites.length, 1);
    });

    test('should have 2 favorites when getNext() is called and both added to favorites', () {
      final myAppState = MyAppState();
      myAppState.toggleFavorite();
      myAppState.getNext();
      myAppState.toggleFavorite();
      expect(myAppState.favorites.length, 2);
    });

    test('should have 1 favorites when getNext() is called twice and one is removed', () {
      final myAppState = MyAppState();
      myAppState.toggleFavorite();
      myAppState.getNext();
      myAppState.toggleFavorite();
      myAppState.toggleFavorite();
      expect(myAppState.favorites.length, 1);
    });
  });

  group('Pages', () {
    test('aaa', () {

    });

    test('bbb', () {

    });


  });
}*/
